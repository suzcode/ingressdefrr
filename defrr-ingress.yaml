apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: defrr-ingress
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
  - host: defrr.com
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: defrr-app-service
            port:
              number: 80