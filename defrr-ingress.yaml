apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: defrr-ingress
  annotations:
    kubernetes.io/ingress.class: gce
    kubernetes.io/backend-config: '{"default": "gcs-backend-config"}'
spec:
  rules:
  - host: defrr.com
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: web-map-http
            port:
              number: 80